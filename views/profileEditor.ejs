<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      body::-webkit-scrollbar {
        display: none;
      }
    </style>
  </head>
  <body>
    <div class="text-white w-full p-10 min-h-screen bg-zinc-900">
      <div class="">
        <div class="w-full flex justify-end">
          <a
            class="bg-red-500 py-2 px-5 inline-block my-1 rounded-lg"
            href="/logout"
            >Logout</a
          >
        </div>
        <h3 class="text-3xl my-5">
          Time to showcase a New Look, <%=user.name%> ❤️
        </h3>
        <div
          class="rounded-lg mb-5 flex w-full h-full justify-start gap-7 overflow-hidden items-center"
        >
          <img
            class="w-72 h-72 rounded-lg object-cover overflow-hidden"
            src="/images/upload/<%=user.profilePic%>"
            alt=""
          />
          <div class="flex flex-col gap-3 justify-end w- full h-72">
            <a
              href=""
              class="py-2 px-5 bg-yellow-500 text-black rounded-[5px]"
              id="inputButt"
              >Upload</a
            >
            <a
              href="/deletePfp"
              class="py-2 px-5 bg-yellow-500 text-black rounded-[5px]"
            >Delete</a>
                <a
                href="/profile"
                class="py-2 px-5 bg-yellow-500 text-black rounded-[5px]"
                >return to Profile</a
                >
            <form
              action="/upload"
              id="myForm"
              method="post"
              enctype="multipart/form-data"
            >
              <input
                type="file"
                class="hidden"
                accept="image/*"
                id="inputField"
                name="pfpImage"
              />
            </form>
          </div>
        </div>
      </div>
      <div>
        <div class="posts mt-20">
          <h3 class="text-zinc-500">Your Posts</h3>
          <div class="postContainer flex gap-5 mt-5">
            <%user.posts.forEach((post)=>{ %>
            <div
              class="post w-[20rem] rounded-lg bg-zinc-800 p-5 border-2 border-zinc-700"
            >
              <h4 class="text-2xl mb-3 text-zinc-300"><%=user.name%></h4>
              <p><%=post.content%></p>
              <small class="mt-5 inline-block text-blue-300"
                ><%=post.likes.length%> likes</small
              >
              <div class="buttns flex justify-between">
                <a class="text-blue-500" href="/like/<%=post._id%>">
                  <%=(post.likes.indexOf(user._id)===-1)? "Like":"Unlike" %> </a
                ><a class="text-zinc-400" href="/edit/<%=post._id%>">Edit</a>
              </div>
            </div>
            <%})%>
          </div>
        </div>
      </div>
    </div>
    <script src="/javascripts/script.js"></script>
  </body>
</html>
