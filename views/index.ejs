<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      body::-webkit-scrollbar {
        display: none;
      }
    </style>
  </head>
  <body>
    <div class="text-white w-full p-10 min-h-screen bg-zinc-900">
      <a
        href="/login"
        class="py-2 px-4 inline-block bg-yellow-500 text-black rounded-lg"
        >Login</a
      >
      <h3 class="text-3xl my-5">Create your own Account</h3>
      <form action="/register" method="post">
        <input
          class="px-3 py-2 outline-none mr-3 bg-transparent border-2 rounded-lg border-zinc-800"
          type="text"
          placeholder="name"
          name="name"
        />
        <input
          class="px-3 py-2 outline-none mr-3 bg-transparent border-2 rounded-lg border-zinc-800"
          type="text"
          placeholder="username"
          name="username"
        />
        <input
          class="px-3 py-2 outline-none mr-3 bg-transparent border-2 rounded-lg border-zinc-800"
          type="email"
          placeholder="email"
          name="email"
        />
        <input
          class="px-3 py-2 outline-none mr-3 bg-transparent border-2 rounded-lg border-zinc-800"
          type="password"
          placeholder="password"
          name="password"
        />
        <input
          class="px-3 py-2 outline-none mr-3 bg-transparent border-2 rounded-lg border-zinc-800"
          type="number"
          placeholder="age"
          name="age"
        />
        <input
          type="submit"
          class="bg-blue-500 rounded-md p-3"
          value="Create Account"
        />
      </form>
      <h3 class="text-3xl tracking-tighter text-white mt-10">
        View our users Profiles
      </h3>
      <div class="users w-full flex flex-wrap gap-4 mt-10">
        <%if(users.length>0){%> <%users.forEach((user)=>{%>
        <div
          class="user w-72 relative border-2 flex flex-col gap-3 border-zinc-800 p-5 bg-transparent rounded-md"
        >
          <a class="w-full h-full absolute" href="/show/<%=user.email%>"></a>
          <img
            src="/images/upload/<%=user.profilePic%>"
            class="rounded-lg w-72 h-72 object-cover"
          />
          <h3 class="text-2xl"><%=user.name%></h3>
        </div>
        <%})%> <%}else{%>
        <h3 class="text-2xl text-zinc-800">No Users Yet</h3>

        <%}%>
      </div>
    </div>
  </body>
</html>
